[STATUS]
last_event = initialized
last_event_at =
running_count = 0
total_epochs = 0

[ACTIVE_EPOCHS]
# Format: epoch_id = name|status|last_event|timestamp|iteration|pid

[COMPLETED_EPOCHS]
# Format: epoch_id = name|status|last_event|timestamp|iteration|pid

[HISTORY]
# Recent lifecycle events (last 10)
# Format: event_timestamp = event|epoch_id|details_json
